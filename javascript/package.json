{
  "name": "quantops",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle --format=esm --minify --outfile=lib/index.js src/index.ts",
    "build:watch": "esbuild --bundle --format=esm --outfile=lib/index.js --sourcemap --watch src/index.ts",
    "test": "tsc"
  },
  "devDependencies": {
    "@types/react": "^18.2.6",
    "esbuild": "^0.17.19",
    "typescript": "^5.0.4"
  }
}
